#include <cstdio>
#include <cstring>

int ls(int argc, char **argv)
{
    // char path_buffer[512];
    // char *path = nullptr;
    // if (argc > 1)
    // {
    //     path = argv[1];
    // }
    // else
    // {
    //     if (int ret = current_dir(path_buffer, sizeof(path_buffer)); ret != OK)
    //     {
    //         printf("list current dir fail ret %d", ret);
    //         exit(0);
    //     }
    //     path = path_buffer;
    // }
    // list_directory_result result;
    // result.cursor.size = 0;
    // result.cursor.cursor = 0;
    return 0;
}