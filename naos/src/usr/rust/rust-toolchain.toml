[toolchain]
# This is only the fallback Cargo environment for host-side checks.  The NaOS
# CMake targets override RUSTC and Cargo with the locally built Rust fork
# stage1 toolchain; they must not consume host rust-src.
channel = "nightly"
components = ["rust-src", "rustfmt"]
profile = "minimal"
